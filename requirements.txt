# InterviewIQ — Core runtime dependencies
pypdf>=5.0.0
Pillow>=10.0.0
python-dotenv>=1.0.0

# Optional: real AI-backed interview questions & answer evaluation.
# The app runs fine without these using built-in heuristics.
# Uncomment to enable, then set AI_API_KEY / AI_MODEL / AI_PROVIDER in .env
# openai>=1.0.0

# Optional: microphone-based speech assistance in the mock interview.
# When SpeechRecognition is not installed the app inserts a practice prompt.
# speechrecognition>=3.10.0
# PyAudio>=0.2.11

# Note: tkinter, sqlite3, json, os, pathlib, re, hashlib, hmac, secrets,
# zipfile, and datetime are part of Python's standard library and do not
# need to be installed with pip.
